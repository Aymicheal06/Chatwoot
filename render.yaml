services:
  - type: web
    name: chatwoot-web
    env: ruby
    plan: free
    branch: develop
    buildCommand: bundle install && yarn install && RAILS_ENV=production bundle exec rake assets:precompile
    startCommand: bundle exec puma -C config/puma.rb
    envVars:
      - key: RAILS_ENV
        value: production
      - key: FRONTEND_URL
        value: https://chatwoot-uxaj.onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: chatwoot-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: chatwoot-redis
          type: redis
          property: connectionString

  - type: worker
    name: chatwoot-worker
    env: ruby
    plan: free
    branch: develop
    startCommand: bundle exec sidekiq -C config/sidekiq.yml
    envVars:
      - key: RAILS_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: chatwoot-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: chatwoot-redis
          type: redis
          property: connectionString

  - type: redis
    name: chatwoot-redis
    plan: free
    ipAllowList:
      - source: 0.0.0.0/0
        description: Allow all IPs

databases:
  - name: chatwoot-db
    plan: free

